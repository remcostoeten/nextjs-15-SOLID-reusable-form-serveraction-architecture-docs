export type NavItem = {
  label: string;
  href: string;
  hasSeparator?: boolean;
  isButton?: boolean;
}

export type Colors = {
  BACKGROUND: string;
  BORDER: string;
  HAS_SEPARATOR: string;
  TEXT: string;
  TEXT_HIGHLIGHT: string;
}
