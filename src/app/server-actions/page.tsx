"use client"

import type { NextPage } from 'next';
import ServerActionsView from '../../components/views/server-actions';

const ServerActionsViewPage: NextPage = () => {
  return (
    <ServerActionsView />
  );
};

export default ServerActionsViewPage;
